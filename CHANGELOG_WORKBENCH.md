# Workbench Change Log

## 0.8.0 — Live Workbench and Bounded Execution Bridge

Added private owner UI, session and CSRF API, immutable task import, Creator reuse, live planning adapter, typed tools, exact one-time approvals, dedicated workspaces, recovery snapshots, qualified command-transport boundary, GCP examination guard, hash-chained evidence, submission generation and lock, test suite, migration, and builder handoff.

Model and command execution remain disabled by default. No production deployment or GCP access is part of this code-authoring release.

